import { hExtraBold } from "@/fonts/fonts"


const Footer = () => {
  return (
    <div style={{position:"sticky"}} className="flex justify-center bg-black">
        <h2 style={{backgroundAttachment:"fixed"}} className={`text-white text-[12rem] ${hExtraBold.className} `}>FOOTER</h2>
    </div>
  )
}

export default Footer